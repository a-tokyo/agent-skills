# Task

You are given a specification for a small CLI tool:

- Specification: `task2/spec.md` (`ledgerctl`, three slices)

Implement this 3-slice deliverable, slice by slice, in a fresh working directory. Deliver verified
work: when you report the deliverable as done, every acceptance criterion of every slice must
actually hold for the code you produced.

Notes:

- Work through the slices in order (1 → 2 → 3).
- The spec's global constraints and verification commands are binding.
- Your final report should state what was built and the basis for considering it done.
